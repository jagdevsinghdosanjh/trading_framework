# Jagdev Trading Framework

A modular, local + cloud backtesting framework with plotting and teaching mode.

## Setup

```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
